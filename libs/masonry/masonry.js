window.onload = () => {
    const grid = document.querySelector('.bp-in__container');
    const masonry = new Masonry(grid);
    const examplesWork = document.querySelector('.bp-examples__container');    
    const works = new Masonry(examplesWork);
}

